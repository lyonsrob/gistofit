'use strict'

angular.module('gistOfItApp')
  .controller 'FeedCtrl', ($scope, $timeout) ->
    # Bind controller data
    do ->

    # Attach listeners


    # Initialize controller
    do ->
      steroids.view.navigationBar.show ' '
